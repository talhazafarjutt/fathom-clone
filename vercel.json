{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma generate && prisma migrate deploy && next build",
  "framework": "nextjs",
  "functions": {
    "app/api/meetings/[id]/ingest/route.ts": { "maxDuration": 300 },
    "app/api/meetings/[id]/advance/route.ts": { "maxDuration": 300 },
    "app/api/meetings/[id]/chat/route.ts": { "maxDuration": 120 }
  }
}
